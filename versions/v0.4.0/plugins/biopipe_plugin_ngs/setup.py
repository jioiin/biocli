from setuptools import setup, find_packages

setup(
    name="biopipe-plugin-ngs",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    description="BioPipe-CLI NGS Plugin"
)
